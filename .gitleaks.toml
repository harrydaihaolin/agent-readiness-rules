# Minimal gitleaks config — satisfies agent-readiness `safety.gitleaks_config`.
# Wire into CI via https://github.com/gitleaks/gitleaks-action as needed.

title = "agent-readiness-rules"

[allowlist]
description = "Per-repo false-positive allowlist"
paths = [
  '''(.*?)(\.lock|\.snap)$''',
  '''node_modules/''',
  '''vendor/''',
]
